// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"yun.tea/block/bright/datafin/pkg/db/ent/filerecord"
	"yun.tea/block/bright/datafin/pkg/db/ent/predicate"
)

// FileRecordQuery is the builder for querying FileRecord entities.
type FileRecordQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.FileRecord
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FileRecordQuery builder.
func (frq *FileRecordQuery) Where(ps ...predicate.FileRecord) *FileRecordQuery {
	frq.predicates = append(frq.predicates, ps...)
	return frq
}

// Limit adds a limit step to the query.
func (frq *FileRecordQuery) Limit(limit int) *FileRecordQuery {
	frq.limit = &limit
	return frq
}

// Offset adds an offset step to the query.
func (frq *FileRecordQuery) Offset(offset int) *FileRecordQuery {
	frq.offset = &offset
	return frq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (frq *FileRecordQuery) Unique(unique bool) *FileRecordQuery {
	frq.unique = &unique
	return frq
}

// Order adds an order step to the query.
func (frq *FileRecordQuery) Order(o ...OrderFunc) *FileRecordQuery {
	frq.order = append(frq.order, o...)
	return frq
}

// First returns the first FileRecord entity from the query.
// Returns a *NotFoundError when no FileRecord was found.
func (frq *FileRecordQuery) First(ctx context.Context) (*FileRecord, error) {
	nodes, err := frq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{filerecord.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (frq *FileRecordQuery) FirstX(ctx context.Context) *FileRecord {
	node, err := frq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FileRecord ID from the query.
// Returns a *NotFoundError when no FileRecord ID was found.
func (frq *FileRecordQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = frq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{filerecord.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (frq *FileRecordQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := frq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FileRecord entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FileRecord entity is found.
// Returns a *NotFoundError when no FileRecord entities are found.
func (frq *FileRecordQuery) Only(ctx context.Context) (*FileRecord, error) {
	nodes, err := frq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{filerecord.Label}
	default:
		return nil, &NotSingularError{filerecord.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (frq *FileRecordQuery) OnlyX(ctx context.Context) *FileRecord {
	node, err := frq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FileRecord ID in the query.
// Returns a *NotSingularError when more than one FileRecord ID is found.
// Returns a *NotFoundError when no entities are found.
func (frq *FileRecordQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = frq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{filerecord.Label}
	default:
		err = &NotSingularError{filerecord.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (frq *FileRecordQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := frq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FileRecords.
func (frq *FileRecordQuery) All(ctx context.Context) ([]*FileRecord, error) {
	if err := frq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return frq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (frq *FileRecordQuery) AllX(ctx context.Context) []*FileRecord {
	nodes, err := frq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FileRecord IDs.
func (frq *FileRecordQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	if err := frq.Select(filerecord.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (frq *FileRecordQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := frq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (frq *FileRecordQuery) Count(ctx context.Context) (int, error) {
	if err := frq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return frq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (frq *FileRecordQuery) CountX(ctx context.Context) int {
	count, err := frq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (frq *FileRecordQuery) Exist(ctx context.Context) (bool, error) {
	if err := frq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return frq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (frq *FileRecordQuery) ExistX(ctx context.Context) bool {
	exist, err := frq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FileRecordQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (frq *FileRecordQuery) Clone() *FileRecordQuery {
	if frq == nil {
		return nil
	}
	return &FileRecordQuery{
		config:     frq.config,
		limit:      frq.limit,
		offset:     frq.offset,
		order:      append([]OrderFunc{}, frq.order...),
		predicates: append([]predicate.FileRecord{}, frq.predicates...),
		// clone intermediate query.
		sql:    frq.sql.Clone(),
		path:   frq.path,
		unique: frq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FileRecord.Query().
//		GroupBy(filerecord.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (frq *FileRecordQuery) GroupBy(field string, fields ...string) *FileRecordGroupBy {
	grbuild := &FileRecordGroupBy{config: frq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := frq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return frq.sqlQuery(ctx), nil
	}
	grbuild.label = filerecord.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.FileRecord.Query().
//		Select(filerecord.FieldCreatedAt).
//		Scan(ctx, &v)
func (frq *FileRecordQuery) Select(fields ...string) *FileRecordSelect {
	frq.fields = append(frq.fields, fields...)
	selbuild := &FileRecordSelect{FileRecordQuery: frq}
	selbuild.label = filerecord.Label
	selbuild.flds, selbuild.scan = &frq.fields, selbuild.Scan
	return selbuild
}

func (frq *FileRecordQuery) prepareQuery(ctx context.Context) error {
	for _, f := range frq.fields {
		if !filerecord.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if frq.path != nil {
		prev, err := frq.path(ctx)
		if err != nil {
			return err
		}
		frq.sql = prev
	}
	if filerecord.Policy == nil {
		return errors.New("ent: uninitialized filerecord.Policy (forgotten import ent/runtime?)")
	}
	if err := filerecord.Policy.EvalQuery(ctx, frq); err != nil {
		return err
	}
	return nil
}

func (frq *FileRecordQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FileRecord, error) {
	var (
		nodes = []*FileRecord{}
		_spec = frq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*FileRecord).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &FileRecord{config: frq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(frq.modifiers) > 0 {
		_spec.Modifiers = frq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, frq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (frq *FileRecordQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := frq.querySpec()
	if len(frq.modifiers) > 0 {
		_spec.Modifiers = frq.modifiers
	}
	_spec.Node.Columns = frq.fields
	if len(frq.fields) > 0 {
		_spec.Unique = frq.unique != nil && *frq.unique
	}
	return sqlgraph.CountNodes(ctx, frq.driver, _spec)
}

func (frq *FileRecordQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := frq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (frq *FileRecordQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   filerecord.Table,
			Columns: filerecord.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: filerecord.FieldID,
			},
		},
		From:   frq.sql,
		Unique: true,
	}
	if unique := frq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := frq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, filerecord.FieldID)
		for i := range fields {
			if fields[i] != filerecord.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := frq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := frq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := frq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := frq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (frq *FileRecordQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(frq.driver.Dialect())
	t1 := builder.Table(filerecord.Table)
	columns := frq.fields
	if len(columns) == 0 {
		columns = filerecord.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if frq.sql != nil {
		selector = frq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if frq.unique != nil && *frq.unique {
		selector.Distinct()
	}
	for _, m := range frq.modifiers {
		m(selector)
	}
	for _, p := range frq.predicates {
		p(selector)
	}
	for _, p := range frq.order {
		p(selector)
	}
	if offset := frq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := frq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (frq *FileRecordQuery) ForUpdate(opts ...sql.LockOption) *FileRecordQuery {
	if frq.driver.Dialect() == dialect.Postgres {
		frq.Unique(false)
	}
	frq.modifiers = append(frq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return frq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (frq *FileRecordQuery) ForShare(opts ...sql.LockOption) *FileRecordQuery {
	if frq.driver.Dialect() == dialect.Postgres {
		frq.Unique(false)
	}
	frq.modifiers = append(frq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return frq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (frq *FileRecordQuery) Modify(modifiers ...func(s *sql.Selector)) *FileRecordSelect {
	frq.modifiers = append(frq.modifiers, modifiers...)
	return frq.Select()
}

// FileRecordGroupBy is the group-by builder for FileRecord entities.
type FileRecordGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (frgb *FileRecordGroupBy) Aggregate(fns ...AggregateFunc) *FileRecordGroupBy {
	frgb.fns = append(frgb.fns, fns...)
	return frgb
}

// Scan applies the group-by query and scans the result into the given value.
func (frgb *FileRecordGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := frgb.path(ctx)
	if err != nil {
		return err
	}
	frgb.sql = query
	return frgb.sqlScan(ctx, v)
}

func (frgb *FileRecordGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range frgb.fields {
		if !filerecord.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := frgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := frgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (frgb *FileRecordGroupBy) sqlQuery() *sql.Selector {
	selector := frgb.sql.Select()
	aggregation := make([]string, 0, len(frgb.fns))
	for _, fn := range frgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(frgb.fields)+len(frgb.fns))
		for _, f := range frgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(frgb.fields...)...)
}

// FileRecordSelect is the builder for selecting fields of FileRecord entities.
type FileRecordSelect struct {
	*FileRecordQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (frs *FileRecordSelect) Scan(ctx context.Context, v interface{}) error {
	if err := frs.prepareQuery(ctx); err != nil {
		return err
	}
	frs.sql = frs.FileRecordQuery.sqlQuery(ctx)
	return frs.sqlScan(ctx, v)
}

func (frs *FileRecordSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := frs.sql.Query()
	if err := frs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (frs *FileRecordSelect) Modify(modifiers ...func(s *sql.Selector)) *FileRecordSelect {
	frs.modifiers = append(frs.modifiers, modifiers...)
	return frs
}
