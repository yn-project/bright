syntax = "proto3";

package basetype;

option go_package = "yun.tea/block/bright/proto/bright/basetype";

enum EndpointState {
    EndpointDefault = 0;
    EndpointAvailable = 10;
    EndpointParse = 20;
    EndpointError = 40;
}

enum AccountState {
    AccountUnkonwn = 0;
    AccountAvailable = 10;
    AccountLow = 20;
    AccountError = 40;
}